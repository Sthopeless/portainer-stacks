---
services:
  adminer:
    image: adminer
    container_name: adminer
    hostname: adminer
    network_mode: host
    ports:
      - ${PORT}:8080
    environment:
      - ADMINER_DEFAULT_SERVER=${ADMINER_DEFAULT_SERVER}
    restart: unless-stopped
