---
services:
  adminer:
    image: adminer
    container_name: adminer
    network_mode: host
    ports:
      - 8080:8080
    environment:
      - ADMINER_DEFAULT_SERVER=${ADMINER_DEFAULT_SERVER}
    restart: unless-stopped
