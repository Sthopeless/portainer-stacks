---
services:
  adminer:
    image: adminer
    container_name: adminer
    network_mode: ${NET_MODE}
    ports:
      - ${PORT}:8080
    environment:
      - ADMINER_DEFAULT_SERVER=${ADMINER_DEFAULT_SERVER}
    restart: ${RST}
